<h1 class="title has-text-centered">Our Amazing Developers</h1>
<section class="section">
  <div class="container">
    <div class="columns is-mobile">
      {{#each reviews as |review|}}
      <h2>
        <i class="fab fa-github"></i> Burgers in question
      </h2>
      <div class="column is-one-fifth">
        <div class="card">
          <div class="card-content">
            <div class="media">
              <div class="media-content">
                <p class="title is-4">{{review.userId}}</p>
              </div>
            </div>
            <div class="content">
              <p>{{review.reviewDetails}}</p>
            </div>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
    <aside class="container" class="container-is primary is-pulled-right ">
      <h2>Give us your feedback</h2>
      <div class="field">
        <h3>Which buger?</h3>
        <label class="label" for="burger">burger</label>
        <div class="control">
          <input class="input" type="review" id="review" name="review" required>
        <label class="label" for="review">review</label>
        <div class="control">
          <input class="input" type="review" id="review" name="review" required>
      </div>
      <div class="control">
        <button class="button is-link">Submit</button>
      </div>
    </aside>
</section>