<section class="section">
    <div class="container">
        <h1 class="title">Delivery Confirmation</h1>
        <div class="content">
            <p>Thank you for choosing {{restaurant.name}}! Your order has been delivered by {{spacemonkey.name}}.</p>
            <img src="{{spacemonkey.image}}" alt="{{spacemonkey.name}}">
            <p>{{spacemonkey.bio}}</p>
            <button class="button is-primary" onclick="window.location.href='/orders/tracking'">Track My Order</button>
        </div>
    </div>
</section>